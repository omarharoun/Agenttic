"""Agent passport (SPEC-2 M16): short-lived Ed25519 credentials bound to
certification evidence, signed action receipts, and delegation chains. Crypto uses
the maintained ``cryptography`` library — never hand-rolled (Hard Rule 27)."""
