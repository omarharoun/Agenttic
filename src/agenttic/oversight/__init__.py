"""Oversight analytics (SPEC-2 M15) — approval-quality process health.

Aggregate process-health metrics over approval events (never individual scoring):
latency distribution, approval rate, override-of-deny rate, post-approval incident
attribution, and a rubber-stamp indicator. Absent oversight may tighten posture,
never relax it (Hard Rule 26).
"""
