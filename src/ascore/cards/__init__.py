"""Agent cards (SPEC-2 M9) — provenance-tracked agent descriptions.

Field taxonomy is generated deterministically from the vendored AI Agent Index
dataset (never hand-transcribed); autofill, autonomy classification, and the
covered-agent detector compute values from Agenttic's own evidence.
"""
