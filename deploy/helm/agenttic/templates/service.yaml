apiVersion: v1
kind: Service
metadata:
  name: {{ include "agenttic.fullname" . }}
  labels:
    {{- include "agenttic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agenttic.selectorLabels" . | nindent 4 }}
