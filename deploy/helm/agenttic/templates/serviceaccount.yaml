{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "agenttic.serviceAccountName" . }}
  labels:
    {{- include "agenttic.labels" . | nindent 4 }}
{{- end }}
